import { Module } from '@nestjs/common'

/**
 * CoursesModule — minimal stub.
 * Legacy Course model is kept for backward compatibility.
 * New content management is handled by ContentModule.
 */
@Module({})
export class CoursesModule {}
