import { Module } from '@nestjs/common'

/**
 * SkillsModule — minimal stub.
 * Skills are managed via Prisma directly in services that need them.
 * Extend this module to add SkillsController / SkillsService as needed.
 */
@Module({})
export class SkillsModule {}
