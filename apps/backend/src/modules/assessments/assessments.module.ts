import { Module } from '@nestjs/common'

/**
 * AssessmentsModule — minimal stub.
 * Assessment models exist in the Prisma schema.
 * Extend this module to add AssessmentsController / AssessmentsService as needed.
 */
@Module({})
export class AssessmentsModule {}
